{
  "timeout_seconds": {
    "type": "int",
    "description": "HTTP 请求超时时间。",
    "default": 15
  },
  "default_country": {
    "type": "string",
    "description": "Steam 商店搜索默认区域。",
    "default": "CN"
  },
  "default_language": {
    "type": "string",
    "description": "Steam 商店搜索默认语言。",
    "default": "schinese"
  },
  "default_history_country": {
    "type": "string",
    "description": "用户未指定区域时，默认查询历史价格的区域。",
    "default": "CN"
  },
  "history_days": {
    "type": "int",
    "description": "小黑盒历史价格查询天数。",
    "default": 720
  },
  "history_event_limit": {
    "type": "int",
    "description": "历史模式显示的最近促销次数。",
    "default": 5
  },
  "global_price_limit": {
    "type": "int",
    "description": "未指定区域时显示的小黑盒各区当前价数量。",
    "default": 10
  },
  "show_api_links": {
    "type": "bool",
    "description": "是否在回复中显示小黑盒接口链接。",
    "default": false
  },
  "llm_provider_id": {
    "type": "string",
    "description": "用于校正游戏名和未知中文地区的 LLM 提供商。",
    "hint": "留空时不调用 LLM，直接使用原始名称和内置别名搜索。",
    "_special": "select_provider",
    "default": ""
  },
  "llm_name_retry_count": {
    "type": "int",
    "description": "名称校正未匹配后的额外 LLM 重试次数。",
    "hint": "实际最多调用 max(配置值, 0) + 1 次；0 或负数仍执行首次校正。",
    "default": 3
  }
}
